apiVersion: v1
kind: Service

metadata:
  name: products-app

spec:
  selector:
    app: products-app

  ports:
    - protocol: TCP
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
