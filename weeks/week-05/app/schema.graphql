# В этом месте вам необходимо: опишите GraphQL схему
type Like {
  id: Int!
  target: String!
}

type Query {
  likes: [Like!]!
  like(id: Int!): Like
}

type Mutation {
  createLike(target: String!): Like!
}
